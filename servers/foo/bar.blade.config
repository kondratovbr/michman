user nginx;
worker_processes auto;

http {
    server {
        listen 80;
        server_name {{ $domain }};

        root /home/michman/{{ $domain }};

        location / {
            include proxy_params;
            proxy_pass http://unix:/run/gunicorn.sock;
        }
    }
}
